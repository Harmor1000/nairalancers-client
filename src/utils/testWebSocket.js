// Simple WebSocket connection test
// Removed in production: WebSocket debugger is disabled.
// Keeping a no-op export to avoid import/bundle errors if referenced.
export const testWebSocketConnection = () => false;
export default testWebSocketConnection;
